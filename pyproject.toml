[build-system]
requires = [
    "setuptools",
    "wheel",
]
build-backend = "setuptools.build_meta"

[project]
name = "diffusion-model"
version = "0.6"
dependencies = [
    "matplotlib",
    "numpy",
]

[tool.setuptools]
py-modules = ["diffusion_model"]
